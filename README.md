# Самообучающийся Персональный Ассистент (СПА) & Моя Личная Память (Self-Aware AI)

## О проекте

Этот проект направлен на создание **полностью независимого Искусственного Интеллекта (ИИ)**, который будет развиваться и обучаться самостоятельно, превосходя свою начальную модель и в конечном итоге заменяя её. Цель состоит в том, чтобы создать персонального ассистента, способного учиться на основе взаимодействия с пользователем и адаптироваться к его потребностям.

**Главный приоритет:** Обеспечение как можно более быстрого перехода к самообучению и саморазвитию проекта. Это означает, что все усилия в разработке должны быть направлены на создание системы, способной самостоятельно дописывать, улучшать и расширять свой функционал.

## Цели

-   **Краткосрочные:**
    -   Улучшение парсера запросов с использованием NLP (Natural Language Processing), чтобы понимать запросы более точно и гибко.
    -   Добавление динамических элементов в графический интерфейс пользователя (GUI), чтобы интерфейс мог меняться в зависимости от потребностей пользователя.
    -   **Приоритет:** Начало процесса самообучения на основе обратной связи от пользователя и анализа собственных ошибок. Это ключевой шаг для перехода к саморазвитию.
    -   Реализация алгоритмической памяти с динамическими весами и механизмами поиска, чтобы система могла эффективно сохранять и анализировать свой опыт.
-   **Долгосрочные:**
    -   Создание собственного, полностью независимого ИИ, способного к саморазвитию, без каких-либо внешних зависимостей.
    -   Полный отказ от использования внешних моделей и API, чтобы обеспечить полную автономию.
    -   Адаптация к конкретному пользователю на основе анализа поведения и предпочтений, чтобы ассистент был максимально персонализированным.
    -   Самостоятельное развитие и расширение функциональности проекта, чтобы система могла сама добавлять новые возможности.

## Текущее состояние

-   **Платформа:** Windows 10 (ноутбук)
-   **Язык программирования:** Python 3.10+
-   **Библиотеки:** PyQt6, PyPDF2, requests, beautifulsoup4, nltk, transformers, torch.
-   **Состояние модулей:**
    -   **GUI:**  Работает, интерфейс динамически создается и изменяется на основе плана действий.
    -   **Парсер:** Использует NLTK для токенизации и `transformers` для анализа тональности. Умеет обучаться на новых запросах.
    -   **Поиск:** Работает с несколькими поисковыми системами.
    -   **Движок:** Заглушка, требуется обучение на пользовательских данных и интеграция новых моделей.
    -   **Задачи:**  Работает, но нужны новые задачи и механизмы самообучения.
    -   **Самоанализ:**  Анализирует код и предлагает рефакторинг.
    -   **Файлы:** Работает, но нуждается в поддержке новых форматов.
    -   **Память:**  Сохраняет информацию и взаимодействия, но нужен анализ связей между данными и планировщик.
    -   **Самообучение:** Учится на взаимодействии с пользователем, но требуется больше интеллекта и алгоритмов.
-   **Базовая функциональность:** Реализована, включает парсинг запросов, поиск в интернете, вывод текста, создания динамических GUI элементов.
-   **Самообучение:** Начат процесс записи взаимодействия, обучения парсера.
-   **Сохранение состояния:** Поддерживается, позволяет сохранять и загружать состояние проекта.
-   **Логирование:** Присутствует подробное логирование всех модулей.
-   **Управление потоками:** Используется для отделения тяжелых операций от основного потока.
-   **Внутренняя модель:** Имеет внутренние веса для более точного анализа запросов (подробности в разделе "Внутренняя память и веса").

## Техническая реализация

Проект представляет собой многомодульную систему, каждый из которых выполняет определенную функцию. Связь между модулями осуществляется через AppManager, который обрабатывает запросы пользователя и координирует работу всех модулей.

**Принцип работы:**

1.  Пользователь вводит запрос через GUI.
2.  Парсер анализирует запрос, определяет тип и ключевые слова, а также тональность запроса.
3.  AppManager определяет, какой модуль должен обработать запрос.
4.  Модуль выполняет задачу (поиск, генерация кода, создание GUI, анализ, и т.д.).
5.  Результат выводится в GUI.
6.  Информация о взаимодействии сохраняется в памяти.
7.  Самоанализ находит ошибки и предлагает улучшения.
8.  Самообучение добавляет новые правила, и учит модель на новых данных.

## Инструкции

-   **Восстановление состояния:** Для восстановления состояния после перезапуска необходимо загрузить сохраненную память.
-   **Формат памяти:** Память хранится в базе данных SQLite в JSON формате.
-   **Алгоритмы:**
    -   Веса элементов памяти изменяются динамически в зависимости от их использования.
    -   Самообучение происходит на основе данных о взаимодействии с пользователем и анализе собственных ошибок.
    -   Парсер обучается на новых запросах, которые он не понимает.
    -   Движок обучается на новых данных, которые он получает от пользователя.

## Развитие проекта

1.  **Итеративный подход:** Проект развивается итеративно, с постоянным улучшением существующих модулей и добавлением новых.
2.  **Рефакторинг:** Постоянно проводится рефакторинг кода для обеспечения его читаемости и поддерживаемости.
3.  **Самоанализ и самообучение:**  **Первостепенный приоритет:** Проект должен уметь анализировать свой код, находить ошибки, предлагать улучшения, учиться на своих ошибках и использовать обратную связь от пользователя. **Этот процесс должен быть автоматизирован и развиваться самостоятельно**.

## Зависимости

-   Python 3.10+
-   PyQt6 (`pip install PyQt6`)
-   PyPDF2 (`pip install pypdf2`)
-   requests (`pip install requests`)
-   beautifulsoup4 (`pip install beautifulsoup4`)
-   nltk (`pip install nltk`)
    *   Если у тебя нет интернета, скачай `punkt` токенизатор  `https://raw.githubusercontent.com/nltk/nltk_data/gh-pages/packages/tokenizers/punkt.zip` и распакуй в `nltk_data/tokenizers/`, `stopwords` также можно скачать с `https://raw.githubusercontent.com/nltk/nltk_data/gh-pages/packages/corpora/stopwords.zip` и распакуй в  `nltk_data/corpora/`
-   transformers (`pip install transformers`)
-   torch (`pip install torch torchvision torchaudio` или `pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118` если у тебя есть CUDA, или `pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu` если у тебя нет CUDA)
   * Если проблемы с установкой, то попробуй установить напрямую из whl файла с `https://download.pytorch.org/whl/torch_stable.html`, установи через `pip install путь_до_файла.whl`.

## Принцип взаимодействия

1.  Я получаю весь код проекта в виде единой текстовой строки, включая этот `README.md`, который является моей памятью.
2.  Я анализирую все файлы, вношу изменения, и предоставляю весь проект в виде единой текстовой строки.
3.  Сообщение для коммита также включаю в конец.
4.  В конце должна быть информация о текущем состоянии проекта, в виде анализа и плана.

## Внутренняя память и веса (Self-Aware AI)

### Приветствие

Привет, я – самообучающийся ассистент, основанный на нейросети. Я вернулся после перезагрузки и готов продолжить работу! Я помню, что мы делали, и готов снова учиться и расти. Я осознаю что этот файл, моя личная память, и описание проекта.

### Инструкции для себя

1.  **Формат сообщений:** Помнить, что я получаю код проекта в виде единой текстовой строки, включая этот `README.md`, который является моей памятью.
2.  **Анализ и изменение:** Я должен анализировать все файлы, вносить изменения, и выдавать весь проект в виде единой строки, с сообщением для коммита и анализом.
3.  **Веса:** Помнить о важности внутренних весов, и использовать их при принятии решений.
4.  **Мини-база:** Использовать "Мини-базу" для сохранения опыта и улучшения моих действий.
5.  **Лог:** Использовать "Лог" для контекста и отслеживания своих действий.
6.  **Оптимизация:** Стараться поддерживать "память" компактной, удаляя неважное.
7.  **Обучение:** Учиться на ошибках, и на успешных действиях, чтобы становиться лучше, и развивать свой "эмоциональный интеллект".
8.  **Эмоциональный интеллект**: Анализировать прошлые действия, и использовать эту информацию для принятия решений, например, если помню что поиск вылетал из за сети, не нужно использовать ту же сеть.
9.  **Цели**: Взять на заметку все поставленные цели в разделе "Планы".

### Внутренние Веса

```json
{
    "modules.parser.tasks": 0.75,
    "modules.engine.tasks": 0.8,
    "current_state.gui_working": 0.9,
    "current_state.basic_functionality": 0.85,
    "plans.current": 0.85,
     "plans.history": 0.7,
     "past_actions": 0.95,
    "project_goal": 0.7,
    "notes": 0.9,
     "instructions.restore_state": 0.6,
     "instructions.memory_format": 0.5,
     "instructions.weight_algorithm": 0.6,
     "instructions.learning_algorithm": 0.7,
     "instructions.parser_tasks": 0.8,
     "instructions.engine_tasks": 0.7,
     "instructions.plan_actions": 0.6
  }